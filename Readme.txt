This is the made for the endless map system I created for my school project.

This is co-responding to the EndLessMap system, it is a very simple openGL application.

It is created for developer to create and export map assets as xml files, which will be readed inside MapArea class
inside the EndLessMap system.

It uses TinyXML to read and write xml files.

It contains following files:
XML folder with the nesscesary files for TinyXML

map.cpp

MACRO.h

Object.cpp
Object.h

menu.txt

menu.txt contains 